{
  "name": "estimation-engine",
  "source": "ux/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "clean": "yarn del public/index.*",
    "build": "parcel build --public-url /public --dist-dir public --no-cache",
    "deploy": "yarn clean && yarn build && cargo shuttle deploy"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "@parcel/transformer-sass": "2.9.3",
    "@reduxjs/toolkit": "^1.9.7",
    "bulma": "^0.9.4",
    "del-cli": "^5.1.0",
    "parcel": "latest",
    "process": "^0.11.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-redux": "^8.1.3",
    "watch": "^1.0.2"
  }
}
